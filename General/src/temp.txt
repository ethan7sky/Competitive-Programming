#include <iostream>
#include <algorithm>
using namespace std;


int main() {
    int x, n; cin >> x >> n;
    // int sticks[n];
    // for(int i=0; i<n; i++){
    //     int temp; cin >> temp;
    //     cin >> sticks[i]
    // }
    // std::sort(sticks, sticks+n);
    // int currLen = 0;
    // int cost = 0;
    // for(int i=0; i<n; i++){
    //     currLen += sticks[i];
    //     cost += currLen;
    // }
    cout << x;
}
